change the project from NextJS to ReactJS. leave the typescript, heroicons, tailwindcss and other dependencies.

but it has to be reactjs.

make sure the dashboards look exactly like "shared/5.png" and "shared/6.png". the colors, layouts, fonts, images.

for fonts, icons, or images check the "public/assets/" folder.

DO NOT USE EMOJIS.

create a custom 404 page.
create a login and signup page that should match the layout of the image in "/shared/7.png"

for the landing page (hero section), make it responsive.
